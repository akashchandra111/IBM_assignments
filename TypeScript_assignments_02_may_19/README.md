#Using these typescript codes

To use these typescript codes please follow these things:
* Install Typescript globally
* Install @types/node globally

Commands:
  npm install typescript -g
  npm install @types/node -g
